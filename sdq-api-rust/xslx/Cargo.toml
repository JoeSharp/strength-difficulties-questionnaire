[package]
name = "sdq-xslx"
version = "0.1.0"
edition = "2024"

[dependencies]
# Internal
sdq-model = { path = "../model" }
sdq-service = { path = "../service" }

# External
calamine = "0.22"
chrono = { version = "0.4.45", features = ["serde"] }
