[package]
name = "sdq-service"
version = "0.1.0"
edition = "2024"

[dependencies]
sdq-model = { path = "../model" }

async-trait = "0.1"
uuid = { version = "1.23.4", features = ["serde"] }
chrono = { version = "0.4.45", features = ["serde"] }
